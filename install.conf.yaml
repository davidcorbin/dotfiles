- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf
    ~/.hyper.js: hyper.js
    ~/.antigenrc: antigenrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]

